html
   head
      title Person
   body
      if(type == "error")
         h3(style = "color:red") #{message}
      else
         h3 name: #{message} name: #{person.name} age: #{person.age} nationality: #{person.nationality}